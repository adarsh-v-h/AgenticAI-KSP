# Local dev: override to hit the live backend directly instead of Vite's proxy.
# For production Slate deploys, VITE_API_BASE_URL is set in Slate's environment
# variables in the Catalyst console — not from this file.
VITE_API_BASE_URL=https://crime-intel-backend-50043099694.development.catalystappsail.in
