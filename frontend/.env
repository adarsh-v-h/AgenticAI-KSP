# Frontend build-time config (Vite).
# VITE_* vars are baked into the static build at `npm run build` time.
#
# This is the live backend (Catalyst AppSail) URL. All contributors build/deploy
# the frontend against the same backend so there is a single shared deployment.
VITE_API_BASE_URL=https://crime-intel-backend-50043099694.development.catalystappsail.in
