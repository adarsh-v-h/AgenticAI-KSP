fastapi==0.115.0
uvicorn[standard]==0.30.6
aiomysql==0.2.0
httpx==0.27.2
orjson==3.10.7
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
pydantic==2.8.2
fpdf2==2.8.7
bcrypt==4.2.0
# Required by routers/voice.py (FastAPI Form/UploadFile) at import time — the
# test suite imports the router, so tests fail to collect without it.
python-multipart==0.0.12

# Testing
pytest==8.3.3
hypothesis==6.112.1

# gRPC
grpcio==1.83.0
grpcio-tools==1.83.0
